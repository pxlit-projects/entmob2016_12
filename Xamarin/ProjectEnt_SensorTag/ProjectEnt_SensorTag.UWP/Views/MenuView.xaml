<Page
    x:Class="ProjectEnt_SensorTag.UWP.Views.MenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjectEnt_SensorTag.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource MainViewModelLocator}, Path=MainViewModel}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="hoogte">
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowHeight="400" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="helloTextBlock.FontSize" Value="30"/>
                        <Setter Target="keuzeTextBlock.FontSize" Value="20"/>
                    </VisualState.Setters>
                </VisualState>
            
            </VisualStateGroup>
            
            <VisualStateGroup x:Name="breedte">
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="750" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                        <Setter Target="infoTextBlock.FontSize" Value="50"/>
                        <Setter Target="tempButton.Margin" Value="250,0"/>
                        <Setter Target="helloTextBlock.FontSize" Value="40"/>
                        <Setter Target="keuzeTextBlock.FontSize" Value="30"/>
                    </VisualState.Setters>
                </VisualState>
                
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="300" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="infoTextBlock.FontSize" Value="30"/>
                        <Setter Target="tempButton.Margin" Value="100,0"/>
                    </VisualState.Setters>
                </VisualState>
                
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>




        <Grid.RowDefinitions>
            <RowDefinition Height="1.2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        
        <RelativePanel Grid.Row="0" BorderBrush="Black">
            <TextBlock Name="helloTextBlock" Text="Welkom Admin" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignHorizontalCenterWithPanel="True"/>
            <TextBlock Name="keuzeTextBlock" Text="Wat wenst u te doen ?" RelativePanel.Below="helloTextBlock" RelativePanel.AlignHorizontalCenterWithPanel="True" />
            <Button Name="tempButton"  RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="keuzeTextBlock" Margin="250,0" Command="{Binding Temp}">Temperature</Button>
            <Button Name="changePasswdButton" Command="{Binding ChangePassword}" RelativePanel.RightOf="tempButton" RelativePanel.AlignTopWith="tempButton">Change password</Button>
        </RelativePanel>

        <RelativePanel Grid.Row="1">
            <TextBlock Name="infoTextBlock" FontSize="40" Text="Hier vindt u de elementen terug:"/>

            <ListView Name="EpisodeList" ItemsSource="{Binding TemperatureList}" RelativePanel.Below="infoTextBlock">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock>
                                   <Run Text="Temperatuur " />
                                   <Run Text="{Binding TemperatureAmount}" Foreground="Blue"/>
                                   <Run Text=" - User : " />
                                   <Run Text="{Binding User.Username}" />
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </RelativePanel>

    </Grid>
</Page>
